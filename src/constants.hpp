#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP


constexpr int G = 1;
constexpr int c = 1;
const double M01 = 1.474 * std::pow(10, 3);
const double G1 = 6.67 * std::pow(10, -11);
const double c1 = 3 * std::pow(10, 8.0);
const double Mev_fm3_to_GU = 1.6 * std::pow(10.0, 32) * G1 / std::pow(c1, 4);
constexpr double pi = 3.14159265358979323846;

#endif